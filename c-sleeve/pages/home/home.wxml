<view>
    <image class="top-theme" src="{{themeA.entrance_img}}"> </image>
    <swiper class="swiper"
            indicator-dots
            indicator-color="#157658"
            autoplay
            circular>
      <block wx:for="{{bannerB.items}}">
        <swiper-item>
            <image class="swiper" src="{{item.img}}"></image>
        </swiper-item>
      </block>
    </swiper>
    <s-category-grid  grid="{{gridC}}"></s-category-grid>
    <image src="{{activityD.entrance_img}}" class="activity"></image>
    <s-spu-scroll wx:if="{{themeE.online}}" 
                class="s-spu-scroll"
                theme="{{themeE}}"
                spu-list="{{themeSpu}}"
                wx:if="{{themeE.online}}">
    </s-spu-scroll>
    <image src="{{themeF.entrance_img}}" class="quaity"></image>
    <s-hot-list banner="{{bannerG}}"></s-hot-list>
    <image src="{{themeH.entrance_img}}" class="fashion"></image>
</view>
